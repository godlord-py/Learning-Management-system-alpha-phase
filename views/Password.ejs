<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewpoint" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <title>Change Password</title>
    <link rel="stylesheet" href="/css/styles.css">
     <script src="https://cdn.tailwindcss.com"></script>
</head>
<style>
    body {
        background-color: #586673;
        display: flex;
        align-items: center;
        justify-content: center; 
    }
    .myclass {
        background-color: #e7f2fd;
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
    }
</style>
<body>
    <div class="grid grid-cols-4 gap-4 p-4 ">
        <div class="col-start-2 col-span-2 rounded-lg ">
            <div class="p-4 bg-blue-600 text-white rounded-t-lg ">
                <%- include('header.ejs') %> </div>
                    <a href="/signout"
                        class=" mt-2 text-green-500 text-2xl font-semibold hover:text-blue-700">Signout</a>
            </div>
            <% if (currentUser.role==="student" ) { %>
                <a href="/student"
                    class="text-red-500 px-1 py-1 font-bold text-2xl hover:text-blue-700 ">Dashboard</a>
                <% } else { %>
                    <a href="/teacher"
                        class="text-red-500 px-2 py-5 font-bold text-2xl hover:text-blue-700">Dashboard</a>
                    <% } %>
                        <div class=" myclass px-1 py-18">
                            <h6 class=" py-1 font-bold text-lg mb-1">Change Password:</h6>
                            <form action="/Password" method="post" autocomplete="on">
                                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                <div class="mb-4">
                                    <label for="email" class="text-black-900">Enter Existing Email: </label>
                                    <input type="email" name="email" id="email"
                                        class="border border-gray-900 text-gray-900 rounded p-2 w-full focus:outline-none focus:ring-2 focus:ring-indigo-200 focus:border-indigo-300">
                                </div>
                                <div class="mb-4">
                                    <label for="password" class="text-black-900">New Password: </label>
                                    <input type="password" name="password" id="password"
                                        class="border border-gray-900 text-gray-900 rounded p-2 w-full focus:outline-none focus:ring-2 focus:ring-indigo-200 focus:border-indigo-300">
                                </div>
                                <div class="mt-6">
                                    <button type="submit"
                                        class="text-white bg-indigo-600 hover:bg-indigo-700 font-semibold rounded px-4 py-2 mb-10 ml-14 transition-all">Set
                                        Password</button>
                                </div>
                            </form>
                            <% if (messages && messages.error && messages.error.length> 0) { %>
                                <% for (var i=0; i < messages.error.length; i++) { %>
                                    <p class="bg-red-200 my-4 px-4 py-2 rounded">
                                        <%= messages.error[i] %>
                                    </p>
                                    <% } %>
                                        <% } %>
                        </div>
        </div>
    </div>
</body>

</html>