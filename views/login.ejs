<!DOCTYPE html>  
<html lang=" "en>
    <head>
        <meta charset = "UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewpoint" content="width=device-width, initial-scale=1.0">
        <meta name="csrf-token" content="<%= csrfToken %>">
        <title>Login</title>
        <link rel="stylesheet" href="/css/styles.css">
        <script src="https://cdn.tailwindcss.com"></script> 
    </head>
    <style>
        body {
            background-color: #686666
        }
    </style>
    <body> 
        <div class = "grid grid-cols-6 mx-auto">
            <div class = "col-start-3 col-span-2">  
                <div class = "text-3xl font-bold text-blue-400 justify-center"<%- include('header.ejs')%> </div>
                <h6 class="py-4 text-2xl">Login into your account:</h6>
                <form action="/logging" method= "post" >
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <div class = "py-3 font-semibold " >
                    <label for="Email">Email:</label>
                    <input type = "email" name= "email" class = "border border-gray-900 text-gray-900 rounded p-4 w-full py-1" required>
                    </div>
                    <div class = "py-3 font-semibold " >
                    <label for="Password">Password:</label>
                    <input type = "password" name= "password" class = "border border-gray-900 text-gray-900 rounded p-4 w-full py-1" required>
                    </div>
                    <div class = "py-3 " >
                    <button type = "submit" class = "px-5 py-1.5 rounded background-color: bg-red-600 text-white font-medium mr-2 mb-2">Login</button>
                    </div>
                </form>
                <% if (messages && messages.error && messages.error.length > 0) { %>
                    <div class="error-message">
                      <%= messages.error[0] %>
                    </div>
                  <% } %>
                <%- include ('footer.ejs') %>
            </div>
        </div>  
    </body>
</html>