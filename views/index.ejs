<!DOCTYPE html>  
<html lang=" "en>
    <head>
        <meta charset = "UTF-8">
        <title>Learning Management System</title>
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewpoint" content="width=device-width, initial-scale=1.0">
        <meta name="csrf-token" content="<%= csrfToken %>">
        <title><%= title %></title>
        <link rel="stylesheet" href="/css/styles.css">
        <script src="https://cdn.tailwindcss.com"></script> 
    </head>
    <style>
        body {
            background-color: #686666;
        
        }
        .myclass2 {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
;

        }
    </style>
    <body> 
        <div class = "myclass2 grid grid-cols-6 px-8 py-8 flex font-semibold flex">
            <div class = "col-start-3 col-span-2 py-4"> 
                <div class = "font-bold text-4xl text-blue-400"> <%- include('header.ejs')%></div> 
                <p class="py-4 text-3xl">New Here? <a href = "/signup" class="text-green-600 text-2xl hover:text-blue-500">Sign-up Now</a></p>
                <p class="py-4 text-3xl">Already have an account? <a href = "/login" class="text-green-600 text-2xl hover:text-blue-500">Login Now</a></p>
               <div class = text-2xl> <%- include ('footer.ejs') %></div>
            </div>
        </div> 
    </body>
</html>
